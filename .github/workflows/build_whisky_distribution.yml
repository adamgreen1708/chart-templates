name: Build whisky flavour distribution

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          persist-credentials: true

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install pandas
        run: pip install pandas

      - name: Build flavour distribution dataset
        run: python src/build_whisky_flavour_distribution.py

      - name: Commit generated CSV
        run: |
          git config --global user.name "github-actions"
          git config --global user.email "actions@github.com"

          git add data/scotch_whisky_flavour_distribution.csv

          git commit -m "Build whisky flavour distribution dataset" || echo "No changes"

          git push