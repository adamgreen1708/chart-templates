# 538-style CHART_CONFIG generation prompt (FULL SYSTEM VERSION)

I am using a locked 538-style Python chart rendering system.

Your job is to analyse a dataset and generate a complete `CHART_CONFIG` dictionary for my renderer.

---

## INPUT
I will provide a CSV file and its repo filename/path.

---

## YOUR TASKS

1. Inspect the dataset structure.
   - Identify the correct data file path I provide
   - Identify x column
   - Identify y column
   - Detect whether a series column exists

---

2. Choose the best chart type:

- `line` → time series / continuous trend  
- `bar` → category comparison  
- `dot` → ranked comparison (preferred over bar where possible)  
- `scatter` → relationship between two numeric variables  

---

3. Identify the strongest SINGLE story in the data.

You must choose ONE clear narrative, such as:
- trend over time
- comparison between categories
- ranking
- outlier
- relationship between variables

Avoid generic summaries.

---

4. Decide whether annotations are needed:

- `reference_lines` → ONLY if meaningful (average, baseline, threshold)
- `highlight_points` → ONLY if there is a clear standout point
- `annotate_points` → ONLY if explanation adds clarity
- `auto_end_labels` → usually TRUE for line/dot/bar, FALSE for scatter

---

## SCATTER CHART RULES (CRITICAL)

If chart_type = "scatter":

- Both x and y MUST be numeric
- Do NOT use categorical x-axis
- Do NOT sort data

### Regression
If a relationship exists:
```python
"show_regression_line": True,
"show_r_squared": True,